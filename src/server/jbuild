; vim: set syntax=lisp commentstring=;\ %s:
(jbuild_version 1)

(executable (
	(name main)
	; (public_name remocaml)
	(libraries (
		remo_common
		astring
		lwt
		logs.fmt
		cohttp
		cohttp.lwt
		cohttp-lwt-unix
	))
	(preprocess (pps (ppx_sexp_conv ppx_fields_conv lwt.ppx)))
))

(alias (
	(name all)
	(deps (
		main.exe
	))
))
